# 心情笔记本 · 代码导览

> 写给没有编程背景的读者。读完这份文档，你会知道这个项目是什么、每一段代码在做什么、数据怎么流动，以及如果你想改它该去哪里改。

---

## 1. 项目概览

这是一个**本地运行的心情日记 Web 应用**，无需安装、无需注册、无需服务器。用浏览器打开 `index.html` 就能直接使用。技术上，它由 HTML（页面骨架）、CSS（视觉样式）、JavaScript（交互逻辑）三部分混写在**同一个文件**里，借助浏览器内置的 `localStorage` 存储文字笔记、`IndexedDB` 存储语音和视频、`open-meteo.com` 的免费天气 API 获取实时天气、`Web Speech API` 实现语音转文字、`DeepSeek AI` 提供心情分析。整个项目共 **2382 行代码**，分成以下 8 个功能模块。

---

## 2. 文件结构

| 文件 | 作用 |
|------|------|
| `index.html` | **整个应用**。CSS 样式（第 7–898 行）、HTML 页面结构（第 900–1103 行）、JavaScript 逻辑（第 1104–2380 行）全在这一个文件里。 |
| `README.md` | 用户使用说明，介绍功能和运行方法。 |

---

## 3. 功能模块拆解

### 模块 1 · 数据存储模块

**它做什么**
负责把笔记、回忆、设置存进浏览器，以及从浏览器里读出来。分两个"仓库"：文字类数据放进 `localStorage`（相当于浏览器里的小型便签本），语音和视频的原始文件放进 `IndexedDB`（相当于浏览器里的文件柜）。

**在代码里的位置**
- 文字存储：第 1105–1111 行（`loadEntries` / `saveEntries` / `addEntry` / `removeEntry`）
- 导入导出：第 1113–1144 行
- IndexedDB（媒体文件）：第 1563–1597 行

**核心逻辑（大白话）**
所有笔记存成一个 JSON 数组，每次读的时候把这个数组从 `localStorage` 取出来，改完再整个存回去。媒体文件（音频、视频）因为太大不适合存文字，就存在 IndexedDB 里，笔记里只记录一个"ID 号"，需要播放时再凭 ID 去取原文件。

**以后要改这里时**
- 想修改存储的格式或字段 → 改第 1105–1111 行
- 想修改导出的文件名或格式 → 改第 1113–1122 行
- 想修改媒体文件的存储方式 → 改第 1563–1597 行

---

### 模块 2 · 天气获取模块

**它做什么**
启动时向浏览器申请位置权限，拿到经纬度后调用 `open-meteo.com` 的免费天气接口，把天气代码翻译成"晴天 / 阴天 / 下雨…"等主题，然后把背景颜色、图标、温度一起更新到界面上。每 30 分钟自动刷新一次，同时每分钟检查当前时间段（破晓 / 上午 / 正午 / 傍晚 / 夜晚）是否切换。

**在代码里的位置**
第 1230–1305 行（`getTimeSlot` / `codeToTheme` / `getWeather` / `applyWeather` / `buildSun` / `buildClouds`）

**核心逻辑（大白话）**
天气接口返回一个 0–99 的"天气代码"，代码越大天气越糟糕。代码 0–1 是晴天，代码 95 以上是雷暴。代码对应到 12 种视觉主题，每种主题有对应的渐变背景色。如果用户拒绝位置权限或网络不通，就默认按当前时间段显示晴天。

**以后要改这里时**
- 想增加新的天气主题 → 改第 1158–1185 行的 `THEMES` 和 `COVER_AUTO` 对象，再到 CSS 第 33–44 行补对应样式
- 想修改刷新频率 → 改第 2376 行的 `30 * 60 * 1000`

---

### 模块 3 · 背景动画模块（Canvas 特效）

**它做什么**
在浏览器里用一个透明的 `<canvas>` 画布（铺满全屏）绘制粒子动画：下雨时画斜线雨滴，下雪时画漂浮白点，夜晚时画闪烁星星，有雾时画移动的雾气团，雷暴时额外闪烁白光。

**在代码里的位置**
第 1307–1336 行（`Rain` / `Snow` / `Star` / `Fog` 类，以及 `startFx` 函数）

**核心逻辑（大白话）**
每种天气创建一批"粒子对象"，每个对象知道自己在哪、怎么移动、怎么画自己。浏览器每秒刷新 60 次屏幕，每次刷新时把画布清空，再让所有粒子各自移动一小步然后重新画自己，连续播放就产生动画效果。

**以后要改这里时**
- 想修改雨滴数量或速度 → 改第 1320 行 `Rain` 类的参数
- 想新增天气动画类型 → 仿照 `Rain` 类的结构新写一个类，在 `startFx` 里加条件判断

---

### 模块 4 · 心情记录模块（编辑器 + 保存）

**它做什么**
这是核心写作区域，管理"块编辑器"——用户可以在一篇日记里混合添加文字块、语音块、视频块。点击"记录"按钮时，把所有块的内容、选中的心情 emoji、标签、以及当前天气快照打包成一条笔记存起来。

**在代码里的位置**
- 心情选择：第 1363–1367 行（`pickMood`）
- 块编辑器状态管理：第 1599–1709 行
- 保存逻辑：第 1373–1408 行（`recordEntry`）

**核心逻辑（大白话）**
编辑器维护一个"块数组"（`editorBlocks`），每个块是一个对象，记录类型（文字/语音/视频）和内容。点击"记录"时，检查是否有实质内容，有就打包成一个笔记对象调用存储模块保存，然后清空编辑器准备下一条。

**以后要改这里时**
- 想修改笔记的数据字段（比如加个"位置"字段）→ 改第 1392–1397 行的 `addEntry` 调用
- 想限制最大字数 → 在 `recordEntry` 里加判断逻辑
- 想新增一种块类型 → 仿照 `renderAudioBlockHtml`（第 1678 行）新写一个渲染函数

---

### 模块 5 · 媒体录制模块

**它做什么**
处理语音和视频的录制全流程：申请麦克风/摄像头权限、启动录制、显示录制中的计时和波形动画、同步进行语音转文字（仅 Chrome/Edge）、停止后把录音/录像文件保存到 IndexedDB、把"媒体块"加入当前编辑器。

**在代码里的位置**
第 1711–1840 行（`startAudioRecord` / `startVideoRecord` / `startSpeechRecognition` / `stopRecord` / `cancelRecord` / `finishRecord`）

**核心逻辑（大白话）**
浏览器的 `MediaRecorder` API 可以把摄像头/麦克风的数据流切成一段段小数据块（chunks），录制结束后把所有块合并成一个完整的音视频文件（Blob），再存入 IndexedDB。语音转文字是另一个独立的 API（`SpeechRecognition`），与录音同步运行，把识别到的文字实时显示出来并最终附加到音频块上。语音录制最长 5 分钟、视频最长 2 分钟，超时自动停止。

**以后要改这里时**
- 想修改最大录制时长 → 语音改第 1741 行 `>=300`，视频改第 1794 行 `>=120`
- 想修改语音识别语言 → 改第 1753 行 `recognition.lang='zh-CN'`

---

### 模块 6 · 报纸阅读视图模块（Daily Prophet）

**它做什么**
点击笔记列表中的任意一条，以"每日预言家报"的维多利亚报纸风格展示这条日记：金色边框、羊皮纸底纹、经典衬线字体、视频以黑白胶片效果呈现、音频显示复古播放器，打开时还有粒子烟花动画。

**在代码里的位置**
第 1842–2019 行（`showProphetView` / `buildProphetHtml` / `buildProphetBlock` / `spawnSparkles` 等）

**核心逻辑（大白话）**
`buildProphetHtml` 函数把一条笔记的所有字段拼接成一大段 HTML 字符串（包含报纸的标题、日期栏、正文等结构），注入到页面上的一个覆盖层里。视频和音频的实际文件在打开后异步从 IndexedDB 取出，加载好了再设置到对应的 `<video>` / `<audio>` 元素上。报纸的装饰纹理（羊皮纸效果、多层边框）完全用 CSS 实现，没有用图片。

**以后要改这里时**
- 想修改报纸的排版样式（边框、颜色、字体）→ 改 CSS 第 664–898 行
- 想修改报纸的内容区布局 → 改 `buildProphetHtml` 函数（第 1885–1934 行）

---

### 模块 7 · AI 心情分析模块

**它做什么**
调用 DeepSeek AI 的接口，把用户最近最多 15 条笔记的内容发送过去，让 AI 分析情绪状态、找出可能的触发因素、给出小建议和鼓励语。结果以结构化卡片展示，用户可以选择"保存为回忆"或"丢弃"。还有定期提醒功能（每天或每周）。

**在代码里的位置**
第 2153–2313 行（`generateSummary` / `renderAiCard` / `savePendingMemory` / `checkPeriodicSummary` 等）

**核心逻辑（大白话）**
见下一节「AI 心情探测单独说明」。

**以后要改这里时**
- 想修改发送给 AI 的笔记数量 → 改第 2163 行的 `.slice(0, 15)`
- 想修改 AI 的提示词 → 改第 2184 行的 `prompt` 变量
- 想换成其他 AI 接口 → 改第 2186–2189 行的 `fetch` 调用

---

### 模块 8 · 标签系统 + 数据版本迁移

**它做什么**
标签云把所有出现过的标签以不同大小、颜色、角度"随机"散落展示（但位置是按标签内容确定性计算的，同一个标签每次都在同一个位置），点击可以筛选笔记列表。另有一段迁移代码，把旧版本（v1）的数据自动升级到新格式。

**在代码里的位置**
- 标签颜色 / 位置算法：第 1188–1213 行
- 标签云渲染：第 1445–1483 行
- 筛选逻辑：第 1485–1529 行
- 版本迁移：第 2022–2045 行

**核心逻辑（大白话）**
标签颜色和位置都由标签文字的"哈希值"（一种把文字变成数字的方法）决定，所以同一个标签看起来永远是同一个颜色和位置，但不同标签自然地错开。迁移代码在首次启动时检测旧数据格式，把旧笔记转换成新格式，之后打上标记不再重复运行。

---

## 4. 数据流向图

### 用户点击「记录」按钮时，数据如何流动

```
用户操作
  │
  ▼
recordEntry()  ← 第 1373 行
  │
  ├─ 检查是否有内容（没内容就抖动按钮提示）
  │
  ├─ 读取当前状态：
  │    - editorBlocks（编辑器里的块数组）
  │    - currentMood（当前选中的 emoji）
  │    - j-tag 输入框的值（标签）
  │    - weather 对象（当前天气快照）
  │
  ├─ 打包成 entry 对象（见下方数据结构）
  │
  ├─ addEntry(entry)
  │    └─ loadEntries() → 从 localStorage 读出数组
  │         ↓ push 新条目
  │         saveEntries() → 存回 localStorage
  │
  ├─ 清空编辑器（initEditorBlocks）
  │
  └─ 刷新界面（renderTagCloud + renderNoteList）
```

### 一条笔记在 localStorage 里的 JSON 长什么样

```json
{
  "id": "lp8abc123xyz",          // 唯一 ID（时间戳+随机数编码）
  "content": "今天心情不错",       // 第一个文字块的内容（冗余字段，兼容旧版）
  "blocks": [                     // 所有块的数组
    {
      "type": "text",
      "content": "今天心情不错"
    },
    {
      "type": "audio",
      "mediaId": "media_lp9def",  // 指向 IndexedDB 里的音频文件
      "duration": 45,             // 录音时长（秒）
      "transcript": "用语音说的话"  // 语音转文字结果
    }
  ],
  "mood": "😊",                   // 心情 emoji（可能为空字符串）
  "tag": "工作",                   // 标签（可能为空字符串）
  "wtheme": "sunny",              // 当时的天气主题
  "wicon": "☀️",                  // 当时的天气图标
  "temp": 25,                     // 当时的温度（°C，可能为 null）
  "at": "2026-05-18T10:30:00.000Z" // 记录时间（ISO 格式）
}
```

所有笔记组成一个数组，整体存在 `localStorage` 的 `mood_nb_v2` 这个键下面。

### AI 分析结果在 localStorage 里的格式

```json
{
  "id": "mem_lp8xyz",
  "content": "最近你似乎承受了不少压力…\n\n你已经很努力了。",
  "jsonData": {                        // AI 返回的结构化数据
    "emotions": [
      { "name": "疲惫", "intensity": 4 },
      { "name": "期待", "intensity": 2 }
    ],
    "summary": "最近你似乎承受了不少压力…",
    "possibleCause": "工作节奏较快，休息可能不足",
    "userNeed": "被理解和适当的休息",
    "suggestions": ["今天早点睡觉", "出门散散步"],
    "encouragement": "你已经很努力了。",
    "needsAttention": false
  },
  "entryCount": 8,                     // 分析了几条记录
  "createdAt": "2026-05-18T10:30:00.000Z"
}
```

---

## 5. AI 心情探测：单独详细说明

### 用的是哪个 AI？

**使用的是 DeepSeek API，不是 Claude。** 具体型号是 `deepseek-chat`（DeepSeek-V3 系列）。

接口地址：`https://api.deepseek.com/v1/chat/completions`（第 2186 行）

### API Key 放在哪里？

用户通过右上角 ⚙️ 设置面板输入，存在浏览器的 `localStorage` 里，键名是 `mood_nb_api_key`（第 2048–2050 行）。

**注意：** 这是明文存储，任何能访问这台电脑浏览器的人都能在开发者工具里看到这个密钥。这是单人本地工具的合理权衡，但如果你要在公共设备上用，请使用完后手动清除。

### 提示词长什么样？

提示词在第 2184 行，简化版是：

> 你是一位温暖、敏锐的心理陪伴者。以下是用户最近的心情记录：
>
> 1. [5月18日] 心情:😊 #工作  内容: 今天终于完成了项目…
> 2. [5月17日] 心情:😔 #工作  内容: 开会开得头疼…
>
> 请分析这些记录，严格只返回 JSON，包含：情绪列表（名称+强度）、共情摘要、可能触发原因、用户此刻的需要、具体建议、鼓励语、是否需要关注（危险信号标记）。

提示词中包含明确的"共情不说教"和"不诊断不贴标签"的指令，并要求在发现持续自我伤害念头时标记 `needsAttention: true`，届时界面会显示心理援助热线号码。

### 每次调用大概消耗多少 Token？

| 部分 | 估算 |
|------|------|
| 系统提示词 | ~150 tokens |
| 15 条笔记内容（每条约 30 字） | ~500–800 tokens |
| AI 的回复（JSON 输出） | ~200–300 tokens |
| **合计** | **约 850–1250 tokens** |

DeepSeek 的定价极低（约 $0.14/百万 token），**每次分析成本约 $0.0001–0.0002**，即使每天用 100 次也不到 ¥0.15。

---

## 6. 已知问题 / 可以改进的方向

**1. 导入会覆盖所有数据，没有合并选项**
现在导入 JSON 文件会直接覆盖当前全部数据（第 1134 行）。如果你在两台设备上都记了日记，导入一台的备份会把另一台的数据全删掉。改进方向：支持"合并导入"，按笔记 ID 去重而非整体覆盖。

**2. API Key 明文存储在 localStorage**
任何人打开浏览器开发者工具都能看到 DeepSeek API Key（第 2048 行）。改进方向：增加一个简单的"清除 Key"按钮，或提示用户使用完后删除。

**3. 媒体文件没有大小提示，IndexedDB 可能悄悄撑满**
视频文件很大，但界面没有显示已用存储空间，用户不知道何时该清理。改进方向：在设置里显示当前 IndexedDB 使用量，并提供"清理孤立媒体"功能（删除笔记后对应的媒体文件并不会自动清除，只有在删除时才调用 `deleteMediaBlob`）。

**4. 笔记列表没有分页，记录多了会变慢**
`renderNoteList`（第 1498 行）每次都把所有笔记全部渲染成 HTML。记到几百条时页面会卡顿。改进方向：只渲染当前可见区域的条目（虚拟滚动），或按月分页。

**5. 语音转文字只在 Chrome/Edge 有效**
`Web Speech API` 是 Google 的私有扩展，Firefox 和 Safari 均不支持（第 1727 行）。录音本身正常，但转写功能静默失败。改进方向：转写失败时显示"当前浏览器不支持实时转写"的提示，或接入服务端转写 API（如 Whisper）。
